module.exports = {
  SP_INSERT_PRIMA: "CALL SP_INSERT_PRIMA(?,?,?,?)",
  SP_UPDATE_PRIMA: "CALL SP_UPDATE_PRIMA(?,?,?,?,?,?)",
  SP_SEARCH_PRIMA: "CALL SP_SEARCH_PRIMA(?,?)",
  GET_PRIMAS: "SELECT * FROM primas",
  GET_PRIMA: "SELECT * FROM primas WHERE id = ?",
};
